# Tools

## macOS/Linux

* `bootstrap.sh` — symlink supported dotfiles into `$HOME` and back up existing targets as `.bakN`
* `update.sh` — git update (`pull --rebase --autostash`)

## Windows

* `windows/setup.ps1` — Windows installer for gVim, Git, and tig config
* `windows/_vimrc` / `windows/_gvimrc` — gVim entry rc files

See `../CROSS_PLATFORM.md`.
